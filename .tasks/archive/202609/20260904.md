# 20260904 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260904-唐启云-发布v1.2.4并推送

> 负责人：唐启云　状态：已完成　范围：swagger2api　完成时间：2026-09-04 15:29

# 发布 v1.2.4 并推送

## 目标

发布 1.2.4：本地移除 @fxri/toolkit 依赖、改用全局 toolkit 的变更随版发布；.tasks 保留继续用全局 toolkit 管理；更新 CHANGELOG 为实际情况后推送远端。

## 实施步骤

1. CHANGELOG 1.2.4 条目按实际情况修订
2. `pnpm build` 回归验证
3. 归档本任务，与发版变更同一提交
4. 提交"文档：发布 v1.2.4"并打 tag v1.2.4
5. `pnpm publish` 发布 npm
6. `git push` 推送 master 与 tag

## 影响范围

- CHANGELOG.md、package.json（版本号 1.2.4）
- .tasks/active 与 archive 目录
- npm 包 @fxri/swagger2api 1.2.4

## 验证与回滚

- build 通过、npm publish 成功、远端 master 与 tag 到位
- 回滚：git revert + 删除 tag + npm dist-tag 处理

---

## 20260904-唐启云-移除本地toolkit依赖

> 负责人：唐启云　状态：已完成　范围：swagger2api　完成时间：2026-09-04 15:05

# 移除项目内 @fxri/toolkit，改用全局 toolkit

## 目标

从项目中移除 `@fxri/toolkit` devDependency，后续工程化能力（任务管理、变更集、发版）统一使用全局安装的 toolkit（1.6.1，与原本地版本一致），消除本地与全局的双份维护。

## 实施步骤

1. `pnpm remove @fxri/toolkit` 移除本地 devDependency 并更新 lockfile（node_modules 精简 133 个包）
2. scripts 中 tasks / tasks:archive / changeset / version 均为裸 `toolkit` 调用，删除本地依赖后自动回落到全局命令，无需改动
3. `toolkit tasks check` 校验本建档（0 error / 0 warn）
4. 验证脚本走全局：`pnpm tasks` 正常输出；`pnpm changeset status` 确认正确透传 changesets（其"无变更集"报错属预期业务状态，非命令缺失）
5. `pnpm build` 回归通过

## 影响范围

- package.json、pnpm-lock.yaml
- 不触及 src/ 运行时代码，CHANGELOG 历史条目与 .tasks/archive 历史记录保持原样

## 验证与回滚

- `pnpm tasks` 与 `pnpm changeset status` 正常执行，确认走全局 toolkit
- `pnpm build` 通过
- 回滚：git 还原 + `pnpm install`

---

## 20260904-唐启云-更新toolkit至1.6.1同步重发v1.2.3

> 负责人：唐启云　状态：已完成　范围：swagger2api　完成时间：2026-09-04 13:10

# 更新 toolkit 至 1.6.1 并发版 1.2.3

## 目标

将 @fxri/toolkit 升级至 1.6.1（含归档双重 CR 修复），同步方弦®注册商标信息与版权年份，并发版 1.2.3。

## 实施步骤

1. `pnpm up @fxri/toolkit@^1.6.1` 更新 devDependency（此前 1.2.2 发版因远端冲突回滚，本次基于 origin/master 最新代码重走）
2. 商标信息同步：README.md、NOTICE、TRADEMARK.md 更新为方弦®注册商标状态
3. 版权年份更新为 2025-2026
4. 用 toolkit 任务能力完成建档、校验、归档
5. 创建 changeset → `toolkit changelog version` 发版 1.2.3
6. 构建、提交、发布、推送仓库

## 影响范围

- package.json、pnpm-lock.yaml
- README.md、NOTICE、TRADEMARK.md
- CHANGELOG.md

## 验证与回滚

- `npx toolkit tasks check` 校验通过
- `pnpm build` 回归通过
- 回滚：git 还原 + `pnpm install`

## 完成说明

- 1.2.2 发版提交已通过 cherry-pick 线性重放嵌入 master（无合并提交，v1.2.2 标签随之重新指向重放后提交），1.2.3 在此基础上独立提交发布
- 1.2.3 与 1.2.2 内容重复属预期（同步发布），CHANGELOG 1.2.3 条目已如实标注同步性质
- 构建通过，版本 1.2.3，待提交、tag v1.2.3、npm publish、推送仓库

---

## 20260904-唐启云-更新toolkit至1.6.1并发版

> 负责人：唐启云　状态：已完成　范围：swagger2api　完成时间：2026-09-04 12:40

# 更新 toolkit 至 1.6.1 并发版 1.2.2

## 目标

将 @fxri/toolkit 升级至 1.6.1（含归档双重 CR 修复），同步方弦®注册商标信息，并发版 1.2.2。

## 实施步骤

1. `pnpm up @fxri/toolkit@^1.6.1` 更新 devDependency
2. 商标信息同步：README.md、NOTICE、TRADEMARK.md 更新为方弦®注册商标状态
3. 用 toolkit 任务能力完成建档、校验、归档
4. 创建 changeset（文档+依赖升级）→ `toolkit changelog version` 发版 1.2.2
5. 构建、提交、推送仓库

## 影响范围

- package.json、pnpm-lock.yaml
- README.md、NOTICE、TRADEMARK.md
- CHANGELOG.md

## 验证与回滚

- `npx toolkit tasks check` 校验通过
- `pnpm build` 回归通过
- 回滚：git 还原 + `pnpm install`

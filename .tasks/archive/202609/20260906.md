# 20260906 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260906-唐启云-新增文档站

> 负责人：唐启云　状态：已完成　范围：docs　完成时间：2026-09-06 20:18

# 新增 VitePress 文档站（参照 toolkit 形态）

## 背景

参照 toolkit 仓库（D:\Work\Dev-Space\fxri\0001.base\toolkit）的 docs 文档站形态，为 @fxri/swagger2api 建一套中文文档站。用户确认：

- 部署：GitHub 与 Gitee 服务器都要自动部署 docs（本项目三端：origin=git.fxy.ink、github=fxri-net/swagger2api、gitee=fxri/swagger2api）
- 素材：从 D:\Base\02.文档\03.设计\图标\方弦\V2\004.基础 选取，风格对齐 toolkit 站点（logo 用方弦图标-迷你-圆形.png，og-image 参照 toolkit 紫底白标样式生成）

## 实施方案

1. package.json：devDependencies 加 vitepress ^1.6.4；scripts 加 docs:dev / docs:build / docs:preview / sync:changelog-doc
2. docs/.vitepress/config.mts：复用 toolkit 配置模式，base 默认 /swagger2api/（VITEPRESS_BASE 可覆盖），zh-CN、本地搜索中文文案、sitemap、editLink 指向 fxri-net/swagger2api；无品牌图则先不含 og:image
3. 内容页（从 README 提炼扩写）：index.md（hero + 特性）、getting-started.md（安装 + 快速上手）、guide.md（三种模式 + 响应提取 + 配置选项）、cli.md（CLI 参数参考）、faq.md（常见问题）、changelog.md（脚本同步）
4. scripts/sync-changelog-doc.mjs：与 toolkit 同逻辑，根 CHANGELOG.md → docs/changelog.md
5. .gitignore：追加 docs/.vitepress/cache、docs/.vitepress/dist
6. 部署 workflow：
   - GitHub：.github/workflows/deploy-docs.yml（master 分支 docs 变更触发，GitHub Pages）
   - Gitee：Gitee Pages 无 API 自动部署（Gitee Pages 付费版才有自动触发），workflow 内以 Gitee Pages API 尝试触发 + 输出人工刷新提示，或仅提供说明；落地时按仓库实际条件处理
7. 素材：logo 复制 方弦图标-迷你-圆形.png → docs/public/logo.png；og-image 用 sharp/代码按 toolkit 风格生成紫底白标图

## 影响范围

- 新增 docs/、scripts/、.github/workflows/（github 目录）；
- 修改 package.json（devDep + scripts）、.gitignore、docs/changelog.md（生成物）
- 不影响 src/ 与 npm 包产物（docs/scripts/.github 均不在 files 白名单）；pnpm-lock.yaml 新增 vitepress 依赖树

## 验证方式

- pnpm install 成功；pnpm docs:build 构建通过；pnpm docs:preview 本地预览页面正常
- pnpm sync:changelog-doc 幂等执行，docs/changelog.md 内容与根 CHANGELOG 一致
- workflow 推送后在 GitHub Actions 观察构建部署结果

## 实施记录（2026-09-06 完成）

- 全部按方案落地：docs 站点 6 页（index/getting-started/guide/cli/faq/changelog）+ config.mts + sync 脚本 + og-image 生成脚本（scripts/generate-og-image.mjs，SVG 模板经 Edge headless 截图导出 1024×512 PNG，风格对齐 toolkit 紫底白标）+ logo 复制（方弦图标-迷你-圆形.png）
- 部署双端落地：GitHub 用 .github/workflows/deploy-docs.yml（参照 toolkit，分支 master）；origin 自建 GitLab 用 .gitlab-ci.yml pages job（node:20 + corepack pnpm，产物 mv 至 public/，仅 docs 相关变更触发）。⚠️ Gitee Pages 无 API 自动触发，未纳入自动部署（Gitee 仓库仍做代码镜像）；自建 GitLab 需确认 Pages 服务已启用，首次部署后在 项目 > Deploy > Pages 查看地址
- 验证通过：pnpm install（vitepress 1.6.4，+120 包）、docs:build 3.79s 全页生成含 sitemap、toolkit tasks check 0 error 0 warn、eslint 0 error、pnpm build 产物无变化（包产物不含 docs/scripts/.github/.gitlab-ci.yml）
- 中间产物 scripts/og-image-template.svg 已 gitignore（可由脚本确定性再生）

---

## 20260906-唐启云-升级glob至v13

> 负责人：唐启云　状态：已完成　范围：swagger2api　完成时间：2026-09-06 19:44

# 升级生产依赖 glob 至 v13

> 延续 20260906-唐启云-治理GitHub依赖漏洞告警（见 archive/202609/20260906.md）

## 目标

消除生产依赖 glob@11.1.0 的 deprecated 状态（官方声明旧版不受支持且含已公开漏洞，修复集中于新版），升级至 ^13.0.6。

## 可行性（已探明）

- glob@13 engines：`18 || 20 || >=22`，与项目 `node >=20` 兼容
- 全仓库仅 [src/index.ts:90](src/index.ts) 一处 `glob.sync(["./**/saconfig*.json"], { ignore: "node_modules/**" })`，为稳定 sync API + ignore 选项，v13 兼容

## 实施步骤

1. `pnpm add glob@^13.0.6` 升级生产依赖
2. `pnpm build` 回归（CJS/ESM/DTS）
3. `pnpm audit` 复验（应维持 1 low 或消除 esbuild 链外无新增）

## 影响范围

- package.json（glob 声明）、pnpm-lock.yaml；不触及 src/ 代码
- 发布产物依赖声明 glob 13.x，要求消费者 node >=18/20（项目 engines 已 >=20）

## 完成说明

- `pnpm add glob@^13.0.6`：11.1.0 → 13.0.6，deprecated 状态消除
- `pnpm build` 回归通过；`pnpm audit` 维持 1 low（esbuild 链，无新增）
- 功能抽查：`glob.sync` 同参数调用正常（返回 []，无异常）
- 发布产物依赖 glob 13.x，与项目 engines >=20 一致

---

## 20260906-唐启云-治理GitHub依赖漏洞告警

> 负责人：唐启云　状态：已完成　范围：依赖治理　完成时间：2026-09-06 19:30

# 治理 GitHub 依赖漏洞告警

## 目标

GitHub Dependabot 报告仓库默认分支 49 个依赖漏洞告警（26 high / 22 moderate / 1 low），需升级依赖修复。判定依据：https://github.com/fxri-net/swagger2api/security/dependabot（推送 v1.2.5 后 GitHub 反馈，时间证据级 1）。

## 建议步骤

1. 拉取告警清单，确认受影响包均属 devDependencies（不影响已发布产物运行时）
2. 优先处理 high 级：升级对应 devDependencies 至修复版本
3. `pnpm install` + `pnpm build` 回归
4. 创建变更集（若涉及产物行为则 patch/minor，纯 devDeps 可不出变更集）按项目发版流程发布
5. 复验 Dependabot 告警清零

## 影响范围

- 仅 devDependencies / pnpm-lock.yaml，不触及 src/ 运行时代码

## 验证与回滚

- 验证：Dependabot 告警清零、build 通过
- 回滚：git 回退依赖升级提交即可

## 完成说明

- 实际执行（用户确认方案 A：semver 内修复 + 必要升级）：
  - `pnpm up`：生产依赖 glob 11.0.3→11.1.0（audit 修复版）；@types/node 刷新；清理 lock 孤儿 @fxri/toolkit
  - `@typescript-eslint/*` 6.21→8.69：estree@6 精确 pin minimatch 9.0.3 无法范围修复，升 8 后依赖 minimatch ^10.2.2 与 tinyglobby 修复版，3 个 high 清零
  - `.eslintrc.cjs` 关闭 v8 recommended 新增的 no-unused-expressions，保持既有短路表达式编码风格，lint 0 error
  - overrides 方案（minimatch/esbuild 压平）在项目环境未触发 lock 重解析，弃用改为版本升级
- 结果：`pnpm audit` 40 → 1（仅 esbuild 0.27.7 low）；build（CJS/ESM/DTS）回归通过
- 残余记录：① esbuild 0.27.7 low（esbuild-plugin-copy 的 peer esbuild 链，Windows dev server 场景，本项目不可达，随后续工具链升级处理）② glob@11.1.0 已标记 deprecated，建议后续评估升 ^13（需核对 engines 与 API 兼容）
- ⚠️ Dependabot 告警需 GitHub 重扫后更新，本地以 `pnpm audit` 为准

---

## 20260906-唐启云-适配toolkit18任务区规范

> 负责人：唐启云　状态：已完成　范围：工程规范　完成时间：2026-09-06 19:30

# 适配 toolkit 1.8 任务区新规范

> 延续 20260905-唐启云-同步全局toolkit技能（见 archive/202609/20260905.md）

## 目标

全局 toolkit 已升至 1.8.1、skills 升 plan-to-task@1.1.1 / session-recap@1.1.0 / release-changelog@1.0.6，项目 `.tasks/` 任务区与新规范存在两处缺口需补齐。

## 现状（2026-09-06 探测）

- CLI 1.8.1 与三 skills 已就位；`toolkit tasks check` / `normalize` 均 0 问题，存量归档块格式兼容
- 缺口 1：`.tasks/conventions.md` 不存在（1.1.x skills 引入的规范沉淀落点，可选）
- 缺口 2：上次收尾的归档变更（active 源文件删除 + archive/202609/20260905.md 归档块更新）未 git 提交入库

## 建议步骤

1. `toolkit init` 补齐 `.tasks/` 骨架缺项（幂等，已存在不覆盖；含 conventions.md 模板与 .gitignore 片段，以实际输出为准）
2. `toolkit tasks check` + `normalize` 复验 0 问题
3. 归档变更随下次任务收尾同批 git 提交（本次收尾已覆盖缺口 2 的入库，执行前先 `git status` 复核）
4. 后续规范提炼统一落 conventions.md（写入前须用户确认）

## 影响范围

- 仅 `.tasks/` 目录骨架与记录；不触及 src/、package.json、依赖

## 验证与回滚

- 验证：check/normalize 0 问题、conventions.md 可见
- 回滚：删除 init 生成的多余文件即可

## 完成说明

- 用户确认执行：`toolkit init` 幂等补齐骨架（仅补 `.gitignore` 的 `.archive.lock` 忽略片段；目录已存在不变；init 不生成 conventions.md，规范落点按需由任务级/会话级提炼创建）
- 缺口 2（归档变更入库）已由 2026-09-06 会话收尾提交 c80b854 覆盖解决
- `toolkit tasks check` / `normalize` 复验均 0 问题
- 无 conventions.md 沉淀需求（会话收尾时用户明确选择不沉淀规范）
